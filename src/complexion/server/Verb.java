package complexion.server;

/**
 * @author head
 * A verb class used for RPCish calling
 * 
 * Might become unnecessary with java reflection.
 */
public class Verb {
	// desc string that tells you what it those.
	String desc;
	// This is where the actually magic happens
	// run the verbs code with a list of Object[] args that allow anything to be passed.
	public void run(Object[] args)
	{
		
	}
}
